<h1>Cadastro</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Gênero</th>
      <th>Tipo</th>
      <th>Data de nascimento</th>
      <th>Contato</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @people.each do |person| %>
      <tr>
        <td><%= person.name %></td>
        <td><%= person.gender %></td>
        <td><%= person.tipo %></td>
        <td><%= person.birthday %></td>
        <td><%= person.contact %></td>
        <td><%= link_to 'Ver', person %></td>
        <td><%= link_to 'Editar', edit_person_path(person) %></td>
        <td><%= link_to 'Apagar', person, method: :delete, data: { confirm: 'Confirma?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Adicionar pessoa', new_person_path %>
